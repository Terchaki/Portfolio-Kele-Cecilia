# Netlify configuration for Angular SPA Portfolio

[build]
  base = "portfolio-kele"
  command = "npm run build"
  publish = "portfolio-kele/dist/portfolio-kele/browser"

# Redirect all requests to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
